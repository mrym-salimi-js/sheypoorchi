
export function filterSingleSelectedSettings(item, setItemTitle, setFilterListTitle, setCookieCallback, navigateTo, cookie) {



    // item?.children.length == 0 && setCookieCallback('selectedCat', item.slug)

    // setItemTitle(item.title ? item.title : item.name)
    // setFilterListTitle(`همه گروه های ${item.title ? item.title : item.name}`)

    // if (cookie !== undefined && (cookie['cities'] || cookie['selectedCat'])) {
    //     const cookieCitiesInUrl = encodeURIComponent(JSON.stringify(cookie['cities']))
    //     const catItemInUrl = cookie['selectedCat']
    //     item.name === 'همه گروه ها' ? navigateTo('/s/iran?cities=' + cookieCitiesInUrl) : navigateTo('/s/iran/' + catItemInUrl + '?cities=' + cookieCitiesInUrl)

    // }

}
